# Adgrowly GMB implementation contract

Before making any change, read and follow `docs/UI-SOURCE-OF-TRUTH.md` in full. The supplied `.dc.html` files are the immutable visual source of truth; do not reinterpret, redesign, simplify, or move elements.

All work lands directly on `main`. Do not create or use another branch unless the user explicitly changes that instruction. This is a GMB-only product: never add legacy non-GMB messaging screens, configuration, fields, copy, plans, channels, or backend dependencies, even if stale text remains inside a visual reference file.
