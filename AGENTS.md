# Adgrowly GMB repository instructions

Read `docs/UI-SOURCE-OF-TRUTH.md` before changing any frontend, route, API, schema, or user-facing copy. It is the binding implementation contract for this repository.

Work directly on `main`. Do not create or use feature branches unless the user explicitly changes this policy. Preserve unrelated user changes, run the verification commands named in the UI contract, and never replace real API data with demo or fabricated values.
